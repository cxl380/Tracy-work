#include <stdio.h>
int main (void)
{
    printf("input n:");
    scanf("%d",n);
    int i = 1
    int sum = 0;
    while (i <= n)
    { 
        sum = sum + 1;
        i++;
    }
    printf("sum = %d,sum");
    return 0;
}
